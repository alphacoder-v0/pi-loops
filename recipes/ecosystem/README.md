# ecosystem — who uses this, and the one word worth sending them

| file | what it is |
|---|---|
| [recipe.toml](recipe.toml) | one job, Mondays at 10:00, `propose` only |
| [scan.md](scan.md) | the playbook: find integrations and forks, draft the one reply or invitation worth sending, file it as a finding |

The level is pinned at `propose` on purpose: every outward word this loop writes is a finding,
and claiming the finding is what sends it. The loop itself never posts anywhere.
