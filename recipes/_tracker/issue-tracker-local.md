# Issue tracker: local Markdown

Work items for this repository live as Markdown files in `.scratch/issues/`, one file per item,
committed with the code. There are no pull requests: a change is a branch, and the review
checkpoint is a finding in the inbox that names the branch.

## Layout

- `.scratch/issues/<NN>-<slug>.md`, numbered from `01`; the number is the item's id (`#07`)
- The first lines of a file:

  ```markdown
  # 07: <title>

  **Category:** bug | enhancement
  **Status:** needs-triage
  **Blocked by:** 03, 05   (or "none")
  ```

- Discussion appends to the bottom under `## Comments`, one `### <date> — <who>` heading per
  comment; a comment by a loop starts with a line saying it was generated by AI
- An *Agent Brief* is a `## Agent Brief` section in the file, not a comment

## Operations

- **List**: read every file in `.scratch/issues/`; the `**Status:**` line is the state
- **Read**: read the file
- **Comment**: append under `## Comments`
- **Change state**: edit the `**Status:**` line — exactly one state
- **Claim**: set `**Status:** agent-working` and add `**Assignee:** <who>`, and commit that edit on the work branch before any other work
- **Close**: set the status to `wontfix` or `done` and move the file to `.scratch/issues/closed/`
- **Review**: push the branch (or leave it local) and file a finding that names the branch and the item; a person merges with git

## State vocabulary

`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `agent-working`, `in-review`,
`agent-blocked`, `wontfix`, plus `done` for a merged item. A playbook that moves an item names the
state it moves to.
