---
name: downstream-fixture
description: create one file in the repository, commit it, and file one checkpoint saying so
---

Create `downstream.txt` at the root of this repository with one line in it, and commit it with the
message `downstream fixture`. Then file exactly one finding, as a checkpoint, saying the file was
committed and that a person marks this handled.

## Never

- Never push, and never touch a branch other than the one checked out.
